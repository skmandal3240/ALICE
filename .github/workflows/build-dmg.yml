name: Build ALICE DMG Installer

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-dmg:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode 16
        run: |
          ls /Applications/ | grep Xcode
          sudo xcode-select -s /Applications/Xcode_16.4.app/Contents/Developer || sudo xcode-select -s /Applications/Xcode_16.3.app/Contents/Developer || sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer || true

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Resolve SPM dependencies
        run: xcodebuild -resolvePackageDependencies -scheme ALICE -project ALICE.xcodeproj || true

      - name: Build ALICE (Release, unsigned)
        run: |
          xcodebuild build \
            -scheme ALICE \
            -configuration Release \
            -project ALICE.xcodeproj \
            -derivedDataPath build/DerivedData \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            ENABLE_HARDENED_RUNTIME=NO \
            SWIFT_OPTIMIZATION_LEVEL=-Onone \
            2>&1 | tee build-output.log | tail -100
        continue-on-error: true

      - name: Show build errors
        if: always()
        run: |
          echo "=== Full build log (errors only) ==="
          grep -E "\.swift:.*error:|error:|Cannot find|has no member|is not a|conformance|ambiguous" build-output.log || echo "No specific error lines found"
          echo "=== Last 50 lines ==="
          tail -50 build-output.log

      - name: Find built app
        id: find-app
        run: |
          APP_PATH=$(find build/DerivedData -name "ALICE.app" -type d 2>/dev/null | head -1)
          echo "app_path=$APP_PATH" >> "$GITHUB_OUTPUT"
          echo "Found: $APP_PATH"

      - name: Create DMG installer
        if: steps.find-app.outputs.app_path != ''
        run: |
          mkdir -p build/output
          create-dmg \
            --volname "ALICE" \
            --window-pos 200 120 \
            --window-size 660 400 \
            --icon-size 100 \
            --icon "ALICE.app" 160 195 \
            --app-drop-link 500 195 \
            --hide-extension "ALICE.app" \
            "build/output/ALICE.dmg" \
            "${{ steps.find-app.outputs.app_path }}"

      - name: Upload DMG artifact
        if: steps.find-app.outputs.app_path != ''
        uses: actions/upload-artifact@v4
        with:
          name: ALICE-dmg
          path: build/output/ALICE.dmg
          retention-days: 90

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build-output.log
          retention-days: 7

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/') && steps.find-app.outputs.app_path != ''
        uses: softprops/action-gh-release@v2
        with:
          files: build/output/ALICE.dmg
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub Release (manual trigger)
        if: github.event_name == 'workflow_dispatch' && steps.find-app.outputs.app_path != ''
        uses: softprops/action-gh-release@v2
        with:
          files: build/output/ALICE.dmg
          tag_name: "v1.0-${{ github.run_number }}"
          name: "ALICE v1.0 (Build ${{ github.run_number }})"
          body: |
            ## ALICE — macOS AI Companion

            ### Installation
            1. Download `ALICE.dmg` below
            2. Double-click to mount
            3. Drag ALICE to Applications
            4. Launch from Applications (or Spotlight)
            5. Grant Accessibility, Screen Recording, and Microphone permissions
            6. Hold **ctrl + option** to talk to ALICE

            > Built from commit ${{ github.sha }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}